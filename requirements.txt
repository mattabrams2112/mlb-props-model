MLB-StatsAPI>=1.7.2
requests>=2.31.0
pybaseball>=2.2.7
# Upper bounds are deliberate. These were unbounded, so a rebuild on 2026-08-10
# pulled pandas 3.0 — which turned a tolerated Timestamp/date comparison into a
# TypeError and silently zeroed out every projection. Raise these bounds only
# after running the worker end-to-end against a real slate.
pandas>=2.0.0,<3.1.0
numpy>=1.24.0,<3.0.0
scikit-learn>=1.3.0
xgboost>=2.0.0
streamlit>=1.35.0
plotly>=5.20.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
lightgbm>=4.0.0
